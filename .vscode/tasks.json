{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pip-install-dev",
            "type": "shell",
            "command": "uv pip install -e \".[dev]\" --python .venv/bin/python",
            "group": "build",
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}
